<section class="py-4 text-center">
  <% if user_signed_in? %>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= render 'shared/statistic_dashboard' %>
    <%= render 'shared/total_dashboard' %>

  <% else %>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= render 'shared/statistic_dashboard' %>
    <%= render 'shared/total_dashboard' %>
  <% end %>
</section>
